#pragma once

#include "Settings.h"

/// @brief Retourne un entier aléatoire entre a et b.
int Int_Random(int a, int b);

/// @brief Retourne un flottant aléatoire entre a et b.
float Float_Random(float a, float b);
